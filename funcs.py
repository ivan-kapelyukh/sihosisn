import historical as hs

def demo_loop(source, target, amount, sell_by, start):
    sell_history = []

    for t in range(start_time, end_time, 24 * 60 * 60):
        sell_history.push((t, amount_to_sell(amount, sell_by, data)))


    # [(time, amount), (time, amount)]
    return []

def amount_to_sell(amount, sell_by, data):
    return
